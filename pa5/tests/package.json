{
  "name": "eai-2026-pa5-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PA5 public black-box tests: HTTP against order-service, AMQP + the RabbitMQ management API against the broker, and the filesystem for notification-service's idempotency state.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "@types/node": "^22.10.2",
    "amqplib": "^0.10.4",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
